# Project config

- **`java-home`** (no extension) — optional. One line: the full path to the JDK to use for this project. Use this on Windows or Linux when the version from `.java-version` isn’t auto-detected. Leave this file out on macOS if you use Homebrew JDKs.
